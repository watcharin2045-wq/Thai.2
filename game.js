const words=['ปลา','แมว','ตา','บ้าน','กา','มือ','ใจ'];for(let i=0;i<15;i++){const d=document.createElement('div');d.className='capsule';d.textContent='🟡';capsules.appendChild(d)}let current='';let score=0;play.onclick=()=>{current=words[Math.floor(Math.random()*words.length)];word.textContent=current;score++;document.getElementById('score').textContent='⭐'+score};speak.onclick=()=>{if(current){const u=new SpeechSynthesisUtterance(current);u.lang='th-TH';speechSynthesis.speak(u)}}